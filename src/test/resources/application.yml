spring:
  datasource:
    url: jdbc:h2:mem:testdb_clases;DB_CLOSE_DELAY=-1
    driverClassName: org.h2.Driver
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  flyway:
    enabled: false # Apagamos Flyway en los test para evitar choques con H2
  h2:
    console:
      enabled: true

eureka:
  client:
    enabled: false